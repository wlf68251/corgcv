-----

未处理：09的全量llm处理




# 当前执行流程

一次运行01-12
然后运行app/app.py

最终运行`streamlit run ./app.py`进行可视化操作

# 部分代码和结果说明

数据应该解压到data/下，是已经经过01、02处理过的数据
已经直接将patecon的代码放在目录下了
当前使用 GDELT QuadClass

patecon源代码似乎没有用到gpu

llm约束只能作为候选约束，不够严格。

# 当前问题1

 GDELT QuadClass分类和cameo顶层分类运行出来的结果不理想：


## 表：两种关系分类方式下的 PaTeCon 约束挖掘结果对比

| 对比项                 | CAMEO 顶层 20 类关系                                         | QuadClass / class 5 类关系                                   |
| ---------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 关系划分方式           | 将 CAMEO 编码映射为 `01`—`20` 共 20 个顶层事件类别           | 将 CAMEO 顶层码进一步合并为 `verbal_cooperation`、`material_cooperation`、`verbal_conflict`、`material_conflict`、`mixed_relation` |
| 输入数据规模           | 267,027 条事实                                               | 140,818 条事实                                               |
| 实体数量               | 6,666                                                        | 7,078                                                        |
| 关系数量               | 20                                                           | 5                                                            |
| 运行参数               | `support=100`，`candidate_confidence=0.5`，`confidence=0.8`  | `support=200`，`candidate_confidence=0.5`，`confidence=0.8`  |
| 总运行时间             | 4,055.54 秒，约 67.6 分钟                                    | 1,245.82 秒，约 20.8 分钟                                    |
| 主要耗时阶段           | `Mutiple Entity Temporal Order Mining`，耗时 3,973.59 秒     | `Mutiple Entity Temporal Order Mining`，耗时 1,146.02 秒     |
| 输出约束数量           | 38 条候选约束                                                | 10 条候选约束                                                |
| 主要约束类型           | 同类关系 `disjoint` 约束                                     | 同类关系 `disjoint` 约束                                     |
| 典型约束形式           | `(a,04,b,t1,t2) & (a,04,c,t3,t4) => disjoint(t1,t2,t3,t4)`   | `(a,verbal_cooperation,b,t1,t2) & (a,verbal_cooperation,c,t3,t4) => disjoint(t1,t2,t3,t4)` |
| 约束含义               | 同一主体对不同客体，或不同主体对同一客体，发生同一类 CAMEO 事件时，时间上通常不重叠 | 同一主体对不同客体，或不同主体对同一客体，发生同一类合作/冲突关系时，时间上通常不重叠 |
| 是否出现跨关系约束     | 本次结果中基本没有明显有效的跨关系 `before/start/include` 约束 | `support=100` 时曾出现少量 `start` / `before` 约束，但置信度或可解释性不足 |
| 语义粒度               | 较细，能够保留 CAMEO 顶层事件语义                            | 较粗，多个不同 CAMEO 事件被合并到同一类中                    |
| 主要问题               | 输出仍以同类 `disjoint` 为主，更多反映事件时间分布规律，而非严格组织关系约束 | 关系过粗，单类事实过于密集，容易导致组合爆炸，且约束解释性更弱 |
| 实验价值               | 可作为 PaTeCon 在事件型时序图谱上的主要测试版本              | 可作为粗粒度关系聚合的对比实验                               |
| 是否适合作为最终主方法 | 不建议直接作为强约束主方法，但可作为弱约束候选来源           | 不建议作为主方法，更适合用于合作/冲突趋势统计或复杂关系标记  |

------

## 表：两种分类方式输出约束的主要规律

| 分类方式               | 主要挖掘到的规律                                             | 示例                                                        | 评价                                                |
| ---------------------- | ------------------------------------------------------------ | ----------------------------------------------------------- | --------------------------------------------------- |
| CAMEO 顶层 20 类       | 同一 CAMEO 顶层关系在相同主体或相同客体条件下通常时间不重叠  | `(a,04,b) & (a,04,c) => disjoint`                           | 语义比 5 类更清晰，但仍主要是事件分布规律           |
| QuadClass / class 5 类 | 同一合作/冲突大类关系在相同主体或相同客体条件下通常时间不重叠 | `(a,verbal_conflict,b) & (a,verbal_conflict,c) => disjoint` | 粒度过粗，容易混合多种事件语义                      |
| CAMEO 顶层 20 类       | 基本未挖掘出稳定的跨关系演化规律                             | 如 `04 before 18`、`10 before 12` 等未明显出现              | 说明 PaTeCon 在事件型数据上难以发现高质量跨关系约束 |
| QuadClass / class 5 类 | 曾出现少量 `start` / `before` 跨关系规则                     | `material_cooperation` 与 `verbal_cooperation` 同时开始     | 可作为弱规律参考，但不适合作为强逻辑约束            |

------

## 可插入报告的总结文字

实验结果表明，CAMEO 顶层 20 类关系和 QuadClass 5 类关系均能够作为 PaTeCon 的输入并完成约束挖掘，但二者输出结果均以同类关系的 `disjoint` 约束为主。CAMEO 顶层 20 类关系保留了更细的事件语义，约束可解释性相对更好；而 QuadClass 5 类关系虽然减少了关系种类，但由于每一类关系内部聚合了大量事件，导致单类事实过于密集，仍然存在较高的计算开销，且输出约束语义更加粗糙。

总体来看，两种分类方式下 PaTeCon 挖掘出的约束更多反映事件型数据在时间窗口中的分布规律，而非组织机构关系图谱中的严格逻辑约束。这说明 ICEWS/GDELT 等事件型时序数据与 PaTeCon 面向持续状态型事实的约束挖掘机制存在一定差异。因此，事件数据更适合作为组织关系图谱的动态更新信号和弱约束候选来源，而不宜直接作为强约束挖掘的唯一依据。