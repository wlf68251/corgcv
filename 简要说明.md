# 当前执行流程

一次运行01-12
然后运行app/app.py

最终运行streamlit run ./app.py进行可视化操作

# 部分代码和结果说明

数据应该解压到data/下，是已经经过01、02处理过的数据
已经直接将patecon的代码放在目录下了
当前使用 GDELT QuadClass

patecon源代码似乎没有用到gpu

llm约束只能作为候选约束，不够严格。

# 当前问题1

 GDELT QuadClass运行出来的结果不理想：


这个结果说明：**5 类 QuadClass 方法已经跑通，但不适合作为 PaTeCon 主实验的理想输入**。它可以作为一个“粗粒度关系实验/对比实验”保留。

## 1. 运行层面：成功了，但很慢

总耗时：

```text
constraint mining running time is: 1245.823 s
```

约等于：

```text
20 分 46 秒
```

其中最耗时的是：

```text
Mutiple Entity Temporal Order Mining time is 1146.018 s
```

约等于：

```text
19 分 6 秒
```

这说明瓶颈几乎全部集中在“多实体时间顺序约束挖掘”。

虽然你把关系压缩到了 5 类：

```text
verbal_cooperation
verbal_conflict
material_cooperation
material_conflict
mixed_relation
```

但是每一类关系内部的事实太密集，导致 PaTeCon 在同类关系内部进行组合比较时仍然非常慢。

所以这验证了之前的判断：

```text
关系数量少 ≠ 一定更快；
关系过粗会导致单类事实过密，也会慢。
```

------

## 2. 输出层面：只产生了 10 条候选约束

候选约束主要是两种形式。

第一种：

```text
(a,R,b,t1,t2) & (a,R,c,t3,t4) => disjoint
```

含义是：

```text
同一个主体 a，在同一类关系 R 下，对不同客体 b、c 的两个事件时间不应重叠。
```

第二种：

```text
(a,R,b,t1,t2) & (c,R,b,t3,t4) => disjoint
```

含义是：

```text
不同主体 a、c，在同一类关系 R 下，对同一个客体 b 的两个事件时间不应重叠。
```

其中 `R` 分别是 5 类 QuadClass 关系。

------

## 3. 语义层面：这些约束价值偏弱

这些约束的置信度都是：

```text
1.0
```

但这不一定说明它们是真实世界中的强规则。

因为你的数据本质上是 ICEWS/GDELT 事件数据，时间多半是单点或由单点构造出的月级/日级区间。PaTeCon 会发现很多事件在时间上“不重叠”，于是得到 `disjoint=1.0`。

但现实中，这类约束并不一定成立。

例如：

```text
A 同一个月内可以同时与 B、C 都发生 verbal_cooperation；
多个国家也可以同一个月内都对同一个国际组织发生 verbal_conflict。
```

所以这些规则更像：

```text
数据分布规律
```

而不是：

```text
严格的组织关系冲突规则
```

尤其是：

```text
(a, verbal_cooperation, b) & (a, verbal_cooperation, c) => disjoint
```

这类规则不能直接解释为“同一主体不能同时与多个对象合作”。这在现实组织关系中明显不成立。

------

## 4. 为什么 support=200 仍然这么慢？

因为 `support` 提高后确实减少了低频候选，但你的 5 类关系过粗，导致每类中仍有大量高频模式。

例如：

```text
verbal_cooperation
```

可能包含：

```text
Make statement
Appeal
Express intent to cooperate
Consult
Diplomatic cooperation
```

大量事件都被归入同一类。
于是 PaTeCon 在计算：

```text
同一个主体下的 verbal_cooperation 事件组合
同一个客体下的 verbal_cooperation 事件组合
```

时，组合数量仍然很大。

这就是 5 类关系反而不理想的原因。

------

## 5. 对这个实验结果应该怎么定位？

建议把它定位为：

```text
QuadClass 粗粒度关系约束挖掘实验
```

而不是最终主实验。

可以在报告中这样写：

```text
本文尝试将 CAMEO 事件编码进一步聚合为 verbal cooperation、material cooperation、verbal conflict、material conflict 和 mixed relation 五类粗粒度关系，并使用 PaTeCon 进行约束挖掘。实验结果表明，粗粒度关系压缩能够减少关系类型数量，但由于每类关系内部事件高度密集，多实体时间顺序挖掘阶段仍存在较高计算开销。同时，输出约束主要集中于同类关系的 disjoint 形式，更多反映事件时间分布规律，而非严格的组织关系逻辑约束。因此，该方法更适合作为粗粒度对比实验或关系复杂性标记方法，而不宜直接作为主要约束来源。
```

------

## 6. 后续建议

你现在可以保留这个 5 类实验，但主线建议转向：

```text
20 类 CAMEO 一级关系 + support=100/200
```

原因是：

```text
20 类比 233 类少，计算压力可控；
20 类比 5 类细，语义更清楚；
输出约束可能更有解释价值。
```

5 类版本适合做：

```text
合作/冲突趋势统计
复杂关系标记
LLM 软规则后验校验
对比实验
```

不太适合做：

```text
PaTeCon 主约束挖掘实验
```

一句话总结：**这次实验跑通了，但结果说明 5 类 QuadClass 粗粒度关系过于宽泛，挖出的 PaTeCon 约束主要是低语义价值的同类 disjoint 规则，建议作为对比实验保留，主实验回到 20 类 CAMEO 一级关系。**